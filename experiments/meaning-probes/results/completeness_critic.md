REPORT.md **does not exist**. `R:/Coding/LinearA/tmp/voynich_repo/experiments/meaning-probes/REPORT.md` is absent, and no `REPORT*.md` exists anywhere in the repo (`find` over the whole tree). So I could not audit report text against results. Everything below is what an audit of `PROTOCOL.md` against the 5 primary JSONs + 34 verification scripts shows the report must contain, and where it will go wrong if written from the JSON verdict strings alone.

**1. REPORT.md missing.** Nothing to review. Protocol §L1 ("The report lists the units on each side"), §Z1 ("The report must state the fraction of numeral controls that themselves clear p < 0.01"), and §"Everything failing → reported as failing, with the measured power of each test" are all unsatisfied by construction.

**2. Z4's PASS — the pass's own p-value comes from an unregistered seed, and the protocol says it must not be claimed.** `t_z4.py:342` is `run_position(items2, 'P2', seed + 1, B)`: every P2 statistic, including the sole passer `MI_P2_F1`, uses **seed 409**, not the registered 408. `results/z4.json` gate: p=0.00159984 vs Bonferroni/6 threshold 0.00166667 — it clears by 7e-5 with MC SE 4.0e-4. `v_z4_null-correctness_decision`: true q≈0.001655, so P(a B=10000 run declares PASS) = **0.413**; observed 6/10 seeds pass (5/9 excluding the registered stream). `v_z4_null-correctness_family` jackknife: dropping any one of 8 of the 12 rings drops it below Bonferroni (p 0.0362 … 0.0047). And `gate.robustness_of_passing_statistic`: FIT p=0.4968, raw-EVA TEST p=0.0826, **GC2a TEST p=0.5660**. PROTOCOL §Replication is unconditional: "A gate that passes on one transcription and fails on the other is reported as transcription-dependent and **is not claimed**." `z4.json` records `outcome: "PASS"` with no such label. The report must not present Z4 as positional structure found.

**3. L1's gate leg 1 is not achievable by its own positive control — the FAIL is uninterpretable as evidence of absence.** Gate needs ≥3 word-final units with |log-odds|≥1 and Bonferroni p<0.01. Culpeper (`l1.json` `culpeper_control.final`) yields **1**: only `p` (+1.287). Voynich TEST yields 2 (`daiin` −1.102 on 4 label/95 text words; `n` +1.586 on **7 label/1 text** words). `gate.measured_power_note` only defends the magnitude leg, never the ≥3 count. The report must state the gate was miscalibrated against the control, not that "no name/grammar split exists."

**4. L1's word-initial family is entirely unregistered, and it is where the only PASS is.** PROTOCOL §L1 freezes "the 25 most frequent word-**final** glyph units (K = 25, frozen)". `l1.json` `gate.GATE_L1_initial_secondary = true` (4 units: `qok` −1.573, `daiin` −1.549, `c` −1.496, `q` −1.213). Any claim from it is stronger than the gate licenses, and it does not survive its own adversarial checks: `v_l1_null-correctness_d` shows `q-` and `qok-` are **one phenomenon** (every such word begins with EVA `q`; merged log-odds −1.687); `daiin-` collapses to the bare word (len≥2 only: 0 label / 1 text, p=1.000); `c-` has a single label token (`cpheey`@f67r2) whose removal leaves 0 label support. `v_l1_confound_2` leave-one-page-out: `daiin` final and initial both fall below |1| when f49v is dropped.

**5. L1's word-final verdict flips on an estimator choice the protocol never fixed.** `v_l1_null-correctness_f`: at kappa=0.5 the pre-registered leg 1 gives **3 units → PASS**; at the kappa=1.0 actually used, 2 → FAIL; uncorrected, 2 → FAIL; flat Haldane-Anscombe 0.5, 1 → FAIL. The word-initial secondary is FAIL under flat HA-0.5 (1 unit). Neither verdict is estimator-stable, and no results JSON records the choice as gated.

**6. `l1.json`'s `estimator` field describes a different estimator than the code implements.** It reads "Mantel-Haenszel pooled log odds ratio … Haldane-Anscombe corrected (0.5 added to every cell)"; `t_l1.py` adds `kappa*m1/n` / `kappa*m0/n` (a marginal correction, kappa=1.0). Flagged in `v_l1_null-correctness_c`; still wrong in the JSON.

**7. R1's ORNAMENTAL pass is a failure-to-reject on 68% of the openers, and the FIT half says the opposite.** `r1r2.json` `runs.ZL3b_comma_split_true.R1.recipesB_TEST_even.bc`: 134 openers → 120 gallows-initial → **92 matchable, 28 dropped** (dropped are the long ones, mean len 7.86 vs 6.52, attested 0.50). b_p=1.000, |diff|=0.0017, and `b_min_detectable_abs_diff_at_p05 = 0.0853` — the test cannot see anything under 8.5 points. Same statistic on **FIT_odd**: b_p=0.0024, −3.17 SD, openers *less* attested; on ALL: p=0.0033, −2.99 SD. `v_r1r2_confound_3` H4 runs 10 nuisance variants: on ALL, 8 of 10 are negative, and the agent's own relaxed match gives TEST −3.56 SD p=0.0008. Awarding "ornamental" for p>0.05 is accepting a null on 92 items; the report must say so and must carry the FIT reversal, not just the headline. Per §Replication it also fails GC2a (`R1_TEST_even_GC2a.ORNAMENTAL_primary_harder = false`).

**8. Z1's "measured power 1.00" is measured 25× above the bar the gate sets.** `z1.json` `gate.measured_power_used = 1.0`, `underpowered = false`. `v_z1_confound_2`: minimum detectable Δ = **+0.04237**, which is 27.5% of the numeral median — *above* the gate's own leg-(ii) threshold of 0.03849. Power **at the gate threshold is 0.42**. `v_z1_confound_4`: a real numeral system occupying 50% of each ring is detected only 40% of the time, 30% only 15%. Also `gate.negative_control_false_positive_rate_same_construction` is 0.50/0.25 under the Δ-only reading, so `measured_power_delta_only_used = 1.0` must not be quoted as power. Z1 FAIL excludes a dominant numeral system, nothing weaker.

**9. Two numbers already caught as wrong in verification must not reappear.** `v_l1_null-correctness_c` quotes a prior report line — *"Culpeper word-initial control: 0 of 25 clear it"* — against `l1.json` `gate.control_units_passing_initial = 2` (`i` −1.249, `o` +1.328). `v_z4_confound_4` F4 quotes *"'ot' is depleted in the first third"* — actual rates by third are 0.132 / 0.213 / 0.128, one-vs-rest p = **0.903**; third 1 is enriched, not third 0 depleted.

**10. R2 under the harder reading could not have produced a survivor, and under the protocol's literal reading FIT and ALL both did.** `r1r2.json` `R2.recipes_TEST_even`: `n_tests_run = 156`, `bonferroni_threshold_actual_tests = 6.41e-5`, `permutation_p_floor = 9.999e-5`, `threshold_below_p_floor = true` — the harder family is a guaranteed null, not a test. Under the protocol's literal 53, TEST has 0 survivors but `recipes_FIT_odd` has `tail × type:shedy` and `recipes_ALL` has `tail × type:shey`. The report must disclose both, not just "nothing survives as registered."

**11. §Replication is unsatisfied for every test, not just declared awkward.** GC2a-n is v101 (digits/capitals as glyphs) and the frozen `clean_tokens` keeps only `[a-z]+`: Z1 `replication_GC2a.status = "NOT RUN"` (largest TEST ring 5 < 8); Z4 `transcription_coverage.GC2a` 91 of 299 loci; L1 `replication_GC2a.validity.valid_replication = false`; R1/R2 `GC2a_replication_status = "NOT RUNNABLE"` with **0 star comments**, so R2 has no annotations to permute. The protocol offers no exemption. Both results that pass (Z4, R1-ornamental) fail their GC2a run. The honest report line is that this pass has no independent-transcription replication at all.

**12. R1(b)/(c) has no glyph-unit-level run.** PROTOCOL §Data: "Every test is run at glyph-unit level and, as a robustness check, at raw EVA-character level." R1's matching and residues are EVA-character throughout (`t_r1r2.py` uses units only for R2's `first_glyph_unit`); `r1r2.json` `runs` varies only corpus and `comma_split`. Z1, Z2/Z3, Z4 and L1 all have both levels; R1 has one.

**13. Minor, but it will be quoted.** `v_z4_null-correctness_family` [d]: `t_z4.py` reports `sorted[4]` of 8 as the numeral "median" (MI_P2_F1: 0.1134 reported, true median 0.0964) — conservative, but mislabelled in `z4.json` `power_numerals`. And `mc_stability_diagnostic.seed_sensitivity_B10000` includes seed 409, which *is* the registered stream, so "6 of 10" is not 10 independent draws.

**Housekeeping:** several verification runs left no artefact in `results/` (`v_z1_null-correctness_1/2/3`, `v_z2z3_confound_1`, `v_z2z3_null-correctness_powercurve/recompute/ttr`, `v_z2z3_reimplementation_b`, `v_r1r2_null-correctness_*`, `v_r1r2_reimplementation_d`), and `v_z1_reimpl_a.json` / `v_z1_reimpl_b.json` sit in the pass root rather than `results/`.

**What is genuinely complete:** Z2's slot conventions A/B/C at unit and char level with numeral controls and a dilution power curve (`_v_z2z3_confound_3`: 5% implantation detected at p=0.0025, so Z2's null is a real constraint); Z3's four registered statistics including the 1,000-draw length-matched TTR resample; Z1's 8 numeral systems + 3 registered negative controls under both cut constructions; R1's `f58_langA_oos` out-of-sample stratum; L1's Culpeper positive control and both robustness levels; and every JSON carries `meaning_lib.sha256` plus corpus hashes with seed 408 and 10,000 draws.