# Research log

## 2026-07-09 — Evidence base and H001

- Imported byte-pinned ZL3b, GC2a, IT2a, RF1b, STA1 alignment files, and Yale's IIIF manifest; recorded URLs, hashes, roles, and rights notes in `data/PROVENANCE.md`.
- Implemented a clean, source-retaining IVTFF parser with explicit uncertainty-preserving analytical views.
- Reproduced the published ZL totals for 5,385 loci, 740 paragraphs, 36,278 long words, and 157,304 STA1 characters. Added locally derived regression counts for 227 IVTFF page/panel records, P/L/C/R = 4,130/1,029/84/142, 817 alternative groups, 2,748 uncertain spaces, 764 drawing boundaries, 446 ligatures, 178 extended-EVA codes, and 39,026 comma-as-boundary tokens.
- H001 reproduced the known leading-gallows paragraph-position enrichment on reserved pages from the same manuscript and ZL3b transcription. It produced no semantic gloss. Of four exploratory exact-token candidates, only `tol` passed the frozen descriptive directional screen; this is not confirmatory evidence.
- Froze H002, a translation-oriented local label-to-prose recurrence test, before execution.
- H002 met its within-ZL numeric criterion, but the held-out partition contained only two page-level evaluation units. Its two hits, `sheol` and `chody` on `f89v2`, fail exact glyph-member identity in the GC2a/STA1 comparison. This is descriptive, not inferential; exact same-page EVA identity was not advanced as a crib.
- H003 retrospectively tested STA-family types co-present in both ZL and GC at aligned loci. Partition B showed 2.42% aggregate enrichment over six pages, but only two page scores were positive versus three negative. The frozen advancement gate failed. Do not advance this crib; the test does not establish absence of family-level recurrence.
- Added the byte-pinned STA1 conversion of IT2a as a reserved third witness. Its outcomes have not been used to select an H004 cross-transcription rule.
- P001 tested an atomic edge-slot parse over strict certain-space STA1 family words. The selected one-prefix/two-suffix inventory produced 69 productive recombinations among 123 unseen affixed test types, but ranked only 2,309th of 5,082 equal-cardinality inventories (54.58th percentile; gate 95th). Disposition: `rejected` as a discriminating morphology route; assign no gloss.
- H004 tested whether the ten-token preceding context predicts the next token's transcriptional form beyond a static matched permutation null. On 33 reserved pages and 2,072 targets, exact-or-one-edit improved by 0.0504 (`p = 0.0001`) and one-edit-without-exact by 0.0379 (`p = 0.0003`); both Currier A/B checks passed. Disposition: `promising` as a structural order effect only. Copy/edit generation remains unvalidated pending discovery-trained order-1/order-2 Markov surrogates and a cross-transcription STA-family robustness check.
- A pre-mechanism audit corrected H004's movable-target definition: a cell now counts only if it contains more than one distinct token type, not merely more than one position. The corrected count is 2,000 of 2,072 (96.53%); the frozen 80% breadth gate remains met and the permutation statistics are unchanged.
- Retracted H004-B-v1 before implementation or outcome inspection. Its greedy without-replacement allocation was not a genuine Markov model conditioned on fixed cell multisets and could starve zero-count candidates. It remains in the tree as a do-not-run audit record while a valid replacement is designed.
- Froze H004-B-v2 before its outcomes: a retrospective teacher-forced comparator using discovery-trained hierarchical order-1/order-2 transition likelihood ratios over each target's exact page/layout-cell distribution. It adds explicit vocabulary coverage and held-out predictive-log-lift validity gates and makes no free-running Markov claim.
- H004-B-v2 completed 9,999 exact teacher-forced replicates per order in 15.894 seconds. Both comparators were invalid, so the frozen disposition is `inconclusive`: baseline `<UNK>` mass was 21.88% overall and 28.88% in Currier A (gate at most 20%), and predictive log lift was negative for every reserved page (page-equal -1.0242 for order 1 and -1.3741 for order 2; A/B target-weighted lifts also negative). Order-1/order-2 `C` deltas were only 0.0081/0.0109 with tail areas 0.2174/0.1379; `U` deltas were 0.0572/0.0631 with tail area 0.0001. Because validity failed, H004-B-v2 neither advances the narrow comparator-failure statement nor rejects H004's `C`/`U` statistics; it does not test free-running Markov generation or reject copying as a possible cause. Generated artifact SHA-256: `01d26e78676f938e010c1d21fd2ce627bf9192932c4dee2fe2bd21a078c381bf`.
- Froze H004-C-v1 before cross-transcription outcome evaluation. It uses strict ordinal locus/component alignment, joint ZL/GC/IT STA-family tuple permutations, an outcome-unexamined IT projection, and explicit attrition/robustness gates. ZL, GC, and IT are correlated views of one manuscript, not independent samples.
- H004-C's pre-IT ZL/GC skeleton retained 38 pages but only 648 targets, including 174 Currier A targets, so it failed the frozen 1,500-total and 200-per-variety breadth minima. The implementation nevertheless completed and exposed the already-inspected 9,999 ZL/GC tuple permutations before a short-circuit correction was requested; those results are retained in the audit manifest. The co-primary effect gates also failed: page-equal `C` deltas were 0.0149 (ZL) and 0.0159 (GC), while `U` deltas were -0.0135 and -0.0124. Disposition: `rejected` as a ZL/GC-robust STA-family route. This stage did not load IT bytes or project IT events; the pinned IT file had only prior integrity/header inspection.

## 2026-07-10 — H005 pharmaceutical plant anchors

- Froze H005 before target-prose scoring: test whether an exact-coordinate `Lf` label retrieves the whole-herbal folio named by a pre-existing q15/q19 visual-source claim. The complete current source ledger contains 35 claims; `data/derived/h005-source-anchor-manifest.json` is frozen at SHA-256 `f6f6f8edd9b21f5aa1e952e26a6104ad327a15a98e426a1fc4ed8e735b9631c7`.
- The physical-folio hash split assigned seven raw claims on six folios to holdout. Exact mapping and strict ZL/GC attrition left five held-out pairs on f19, f43, f44, f48, and f57: the second f48 claim failed ZL/GC family agreement and the f4 claim had no `Lf` locus. Overall, 17 exact-coordinate queries survived on 16 target folios and 11 pharmaceutical pages; the separately flagged row-order mapping was excluded to enforce the frozen coordinate rule. All breadth gates still passed.
- Discovery selected boundary 2/3-gram BM25 model M1 (discovery conservative MRR 0.08535222 versus M0 0.03910457). Held-out conservative MRR was 0.33991087 against selection-adjusted matched-null 0.19643035, delta 0.14348052 and one-sided `p = 0.0568`; top-three was 2/5 against null mean 0.15847585, `p = 0.1592`. Q15 held-out MRR delta was 0.25458515, while q19 was negative at -0.02317642. Every frozen statistical advancement check failed.
- The identical stable form `AQABBA` occurs in claims targeting different whole-herbal folios f44r and f57r, so the predeclared distinct-target-folio identity gate also failed. This is not a botanical species judgment and no blinded image gold exists.
- Disposition: `rejected` as the frozen q15/q19 plant-identity label retrieval route. IT target prose was neither opened nor parsed, and no referential, species, phonetic, or translation gloss advanced. Machine-readable result SHA-256: `04950ee32f1a68c53efb9be7a921a2834f36109f89cefbe8de7eb242fadad179`.

## 2026-07-10 — H006 fixed-three-trit line frame

- Froze H006-FR3-LINE-v1 before any exact-protocol residue at protocol SHA-256 `88e75dd64fa0af6554a613b7d0d1120619f63d18785133febfeb0bfabb0ed96f`. It tests one narrow Matlach-inspired conjunction: two explicit recursive component tables, fixed three-trit blocks, and independent phase-zero reset on every clean IVTFF logical prose line. The gate is deterministic: every eligible line must have expanded primitive count divisible by three; one contradiction kills a model.
- The pre-expansion guard reproduced 1,185 exact ZL3b/IT2a consensus lines over 92 physical folios. Discovery contained 919 lines over 69 folios and passed every frozen Currier, hand, and folio breadth minimum. The holdout inventory contained 266 lines over 23 folios.
- `PAIR` produced residue histogram 0/1/2 = 321/297/301, hence 598 contradictions. `OPTIONAL_E` produced 326/303/290, hence 593 contradictions. Neither model met the exact discovery condition.
- Disposition: `rejected` for H006-FR3-LINE-v1. Held-out primitive counts and residues were not computed or reported, no key/plaintext stage was authorized, and no decoded string, language model, dictionary, gloss, or translation was emitted. This does not reject continuous or paragraph resets, other component tables, variable-length codes, historical homophony, or a Naibbe-like verbose cipher. Machine-readable result SHA-256: `192845597048b3655debadd50661bebdf08b741bb28ecb23146811a59fc963c7`.

## 2026-07-10 — H007 published Naibbe fixed-key coverage

- Froze H007-PK23-COV-v1 before any manuscript token met the Naibbe table, at protocol SHA-256 `87dd3743413d8eeed08dd7f9eec4bd05dd25413a14fabb3a473d446a56c01501`. The route applies the author's published reverse table and reverse-decoder control flow literally with `BASIC=True` and `MARK_COMPOUND=True`; it performs no key search, permutation, table completion, respacing, or exception fitting.
- The table-independent guard reproduced 526 exact ZL3b/IT2a consensus herbal prose lines over 63 physical folios: 2,341 discovery occurrences in 1,024 types and 777 sealed holdout occurrences in 451 types. All corpus hashes, alphabets, byte round trips, strata, folio assignments, and type-overlap counts matched the frozen inventory before table load.
- Stage 0 reproduced all three pinned author-decoder outputs byte-for-byte under Python 3.12.13 and pandas 2.2.3. A table-only synthetic audit compared the class observer with the author's control flow on 124,669 generated strings and found zero disagreements; an independent code review also passed.
- Discovery aggregate classes were `U=797`, `B1=937`, `BA=82`, `C1=27`, `CA=3`, and `X=495` occurrences. The exact 100% generator-membership and 100% unique-output gates both failed. One `X`, `BA`, or `CA` would have sufficed; no error budget or repair was allowed.
- Disposition: `rejected` for the direct published fixed key on this frozen corpus and tokenization. The 777-occurrence holdout remained unevaluated by the table, and no plaintext label, candidate, decoded string, language score, gloss, or translation was released. This does not reject other Naibbe keys, plaintext-letter permutations, repaired decoders, transcriptions, or segmentations. The canonical JSON and a byte-identical audit rebuild have SHA-256 `174e0469312cfbe2d19fe8ad8f435fbd6453a7af9ba1305300140757519b46fd`.
- A post-outcome no-run audit killed the obvious arbitrary-compound repair as a route to a unique reading without inspecting token-level results. Every one of H007's 82 aggregate `BA` occurrences already supplies at least two valid one-atom label streams in the raw forward multimap; recursive deleted-boundary closure can only add alternatives. A table-only audit found 18,947 atomic strings, of which 14,648 also have a distinct multi-atom decoding, with a shortest collision length of two ciphertext characters. This deck-agnostic proof does not reject the exact randomized encoder, but it blocks plaintext release and forbids choosing branches with the Latin controls.

Commands:

```sh
python3 -m unittest discover -s tests -v
python3 scripts/build_folio_index.py
python3 scripts/analyze_corpus.py
python3 scripts/analyze_edge_morphology.py
python3 scripts/analyze_local_order.py
python3 scripts/analyze_contextual_comparator.py
python3 scripts/analyze_h004c_zg.py
python3 scripts/analyze_h005_plant_anchors.py
python3 scripts/analyze_h006_fr3.py
python3 scripts/analyze_h007_pk23.py
```
