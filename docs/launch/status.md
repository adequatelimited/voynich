# Launch status

The [founding AI-scored pilot release](../../grading/releases/2026-09-07-ai-pilot.md) authorizes supported PRs to be graded, merged and credited using the public active profile. The larger calibration cohort remains incomplete and is not represented as passed. Deployment status and actual accepted decisions must be checked on the live service and signed ledger; this release record does not itself award points.

The implementation release is **prelaunch**. The public research package, website and private attendant are being integrated. No official competitive points or translation success are claimed. The initial artifacts are operator-curated seeds; existing seed value must be assessed and occupied before ranked extensions can receive marginal credit.

Implemented research material: 24 source records; two independently produced, provenance-pinned CC0 transliterations; 8 research branches; 14 methods; 18 starter tasks; 12 bounded claims; 4 candidate directions; five baseline analyses executed across three declared normalization variants; compact summaries; two explicitly partial replication reports. Source access gaps, normalization limitations and the distinction between local reproduction and independent scientific replication remain visible.

Implemented software: the public scoring contracts, full rubric and prompts, strict manifests, cumulative family ledger, attribution and correction arithmetic, local grading preview adapter, scientific CPU harness, contributor policies, website, GitHub identity/PR workflow, bounded private assessment orchestration, signed projections and an ephemeral GitHub Actions runner option. Synthetic tests do not constitute real-model calibration.

Activation gates still require recorded evidence:

1. A verified subscription-authenticated native Claude runtime and finite operator allocation. No API or paid-extra fallback is configured.
2. The exact model/profile/prompt release and actual calibration runs, including frozen AI references and separate actual Sonnet evaluation trials; no human reference panel.
3. Successful real fork/PR/review/merge/decision/attribution and recovery pilot under the bounded allocation.
4. Prospective seed-family baselines, protected authority delegation and public signing-key provenance.
5. Verified scientific execution/inspection capacity for artifact types beyond the initially bounded text profile; missing evidence cannot be silently omitted.
6. Independent reproduction and the remaining source/image/rights mappings identified in the research gaps register.

Deployment and verification evidence belongs in [acceptance.md](acceptance.md); operational responsibilities are in [operator-handoff.md](operator-handoff.md). A live website does not by itself satisfy the ranked-challenge gates.
